export interface Game {
  id: number;
  title: string;
  short_description: string;
  thumbnail: string;
  genre: string;
  platform: string;
  // Agrega aquí otros campos según la respuesta de la API
}
